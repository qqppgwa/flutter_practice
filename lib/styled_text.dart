import 'package:flutter/material.dart';

class StyledText extends StatelessWidget {
  const StyledText({key}) : super(key: key);
  @override
  Widget build(context) {
    return const Text(
      'gwaqwq',
      style: TextStyle(color: Color.fromRGBO(5, 1, 1, 1)),
    );
  }
}
